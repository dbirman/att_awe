function [ output_args ] = getThresholdEstimate( input_args )
%GETTHRESHOLDESTIMATE Estimate of threshold with standard dev
%   Tries to estimate the threshold from multiple runs in the most accurate
%   way possible (weibull estimate). If that fails it reverts to using
%   reversals to make an estimate. In either case 
%
end

